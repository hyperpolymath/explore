*PSR 15* is a set of common interfaces for HTTP server request handlers
("`request handlers`") and HTTP server middleware components
("`middleware`") that use HTTP messages as described by PSR-7 or
subsequent replacement PSRs.
